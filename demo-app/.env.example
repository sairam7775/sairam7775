# Same service account JSON used by firebase-campaign-tool — needed here only for
# the /api/subscribe endpoint, which calls admin.messaging().subscribeToTopic().
FIREBASE_SERVICE_ACCOUNT_PATH=../firebase-campaign-tool/service-account.json
PORT=5173
